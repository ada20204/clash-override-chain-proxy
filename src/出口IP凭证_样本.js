/**
 * 出口IP凭证注入脚本
 * 将家宽IP和自定义出口的凭证注入到 config，供完全覆写脚本读取。
 * 所有配置项均为可选，按需取消注释即可。
 *
 * 使用方式：
 *   1. 复制本文件并重命名为「出口IP凭证.js」
 *   2. 按需取消注释并填入实际地址和端口
 *   3. 在 Mihomo Party 覆写列表中启用，且排序必须在「完全覆写.js」之前
 */
function main(config) {

  // ── 家宽IP（可选）─────────────────────────────────────────────────────────
  // 配置后，脚本会注入一个家宽IP节点（通过 dialer-proxy 链式代理出站），
  // 并创建「家宽IP出口」组，AI 和浏览器进程的流量将走这条链路。
  // 不配置则脚本仅做分流覆写，不注入家宽链路。

  // config._homeProxy = {
  //   type: "http",   // http 或 socks5
  //   server: "",     // 家宽 IP 地址（公网 IP 或内网 IP）
  //   port: 1080      // 代理端口
  //   // 如需认证，取消注释以下两行：
  //   // username: "",
  //   // password: ""
  // };

  // ── 自定义出口（可选，数组）───────────────────────────────────────────────
  // 注入后，所有节点会出现在「节点选择」和各功能组中，可按组单独选择出口。
  // 支持单个节点或多个节点。

  // config._customProxies = [
  //   {
  //     name: "自定义出口",    // 节点显示名称，可自定义
  //     type: "http",          // http 或 socks5
  //     server: "",            // 代理服务器地址（IP 或域名）
  //     port: 3128             // 代理端口
  //     // 如需认证，取消注释以下两行：
  //     // username: "",
  //     // password: ""
  //   }
  //   // 如需添加更多节点，继续在此追加：
  //   // ,{
  //   //   name: "备用出口",
  //   //   type: "socks5",
  //   //   server: "",
  //   //   port: 1080
  //   // }
  // ];

  return config;
}
